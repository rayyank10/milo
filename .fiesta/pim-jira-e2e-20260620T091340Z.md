# Preflight Modal Redesign Plan

- Run: `fiesta-jira-pim-e2e-20260620T091340Z`
- Jira issue: `PIM-MILO-PREFLIGHT`
- PIM org: `milo`
- PIM project: `project-harness-work-766e86`
- Scope: `frontend`
- Run kind: `demo`
- Planner code context: `repo_clone`

## Ticket

Goal: Completely redesign preflight modal to use light theme following Adobe best practices, leveraging Milo/c2 design system components. Work completed: reviewed current preflight modal implementation across CSS/JS files; investigated Milo design tokens and c2 components; analyzed markup structure and button generation patterns; created preflight-redesign-light-theme branch; identified mask-image patterns. Key decisions: transition from dark theme to light theme aligned with Milo design system; reuse existing c2 button styling rather than custom preflight-action classes; preserve core modal behavior and accessibility while updating presentation. Next steps: develop CSS updates using Milo design tokens; create mockup; implement backward-compatible changes; test browser/device combinations. Technical considerations: custom clip-path notched appearance, preflight-specific button classes, accessibility features.

## Code-Level Planning Inputs

The planner phase was configured to pull repository context directly from the target repo instead of relying on Scout.

Likely files to inspect or change:
- libs/blocks/preflight/preflight.css
- libs/blocks/preflight/panels/general.js
- libs/utils/preflight-notification.js
- libs/blocks/preflight/panels/merch.js
- libs/blocks/preflight/preflight.js
- libs/blocks/preflight/panels/seo.js
- libs/blocks/global-navigation/global-navigation.js
- libs/blocks/preflight/checks/captureMetrics.js

PIM learning ids referenced by the planner:
- kn-5c9943ec
- kn-c40c8a43
- kn-dcf1b295

## Implementation Direction

- Preserve current preflight modal behavior and accessibility semantics while changing presentation.
- Move the modal toward a light Milo-aligned surface using existing tokens and component patterns.
- Prefer c2/Milo button conventions over preflight-specific action button styling where the markup allows it.
- Treat the notched clip-path and mask-image behavior as compatibility-sensitive until verified in the real modal.
- Test desktop and mobile render states, including focus, error, and action button flows.
